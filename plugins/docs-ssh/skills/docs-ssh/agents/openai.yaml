interface:
  display_name: "docs-ssh"
  short_description: "Inspect mounted docs, issues, and task results over SSH"
  default_prompt: "Use $docs-ssh to inspect /README.md, /projects/<slug>/issues, and /projects/<slug>/docs, then save research or work results under /projects/<slug>/tasks."

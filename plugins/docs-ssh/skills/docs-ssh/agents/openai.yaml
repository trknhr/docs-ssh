interface:
  display_name: "docs-ssh"
  short_description: "Inspect mounted docs and workspace files over SSH"
  default_prompt: "Use $docs-ssh to inspect /README.md and /project/docs, then save project notes under /project/tasks."

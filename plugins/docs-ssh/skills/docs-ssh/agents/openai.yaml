interface:
  display_name: "docs-ssh"
  short_description: "Access project files over authenticated HTTPS"
  default_prompt: "Use $docs-ssh to search, read, and update the configured project workspace over authenticated HTTPS."

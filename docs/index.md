# docs-ssh Sample Docs

This directory is mounted into the SSH session as:

- `/project/sources/<name>`
- `/project/docs`

Try:

```bash
ls /project/docs
cat /project/docs/index.md
grep -R "sample" /project/docs
```
